"use strict";

module.exports = function applyPatch(document, patch) {
  var override = Object.keys(patch).reduce(function (props, key) {
    props[key] = patch[key].$set;
    return props;
  }, {});
  return Object.assign({}, document, override);
};